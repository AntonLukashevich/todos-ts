import React from "react";

import { StyledHeader } from "./styled";

// Aliona - feel free to create new components (tags) with styled-components and put them inside <StyledHeader> based on your need
const Header = () => {
  return <StyledHeader>Some content</StyledHeader>;
};

export default Header;
